<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css" integrity="sha384-MCw98/SFnGE8fJT3GXwEOngsV7Zt27NXFoaoApmYm81iuXoPkFOJwJ8ERdknLPMO" crossorigin="anonymous">
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/js/bootstrap.min.js" integrity="sha384-ChfqqxuZUCnJSK3+MXmPNIyE6ZbWh2IMqE241rYiqJxyMiZ6OW/JmZQ5stwEULTy" crossorigin="anonymous"></script>
    <title>Wedding Planner</title>
</head>
<body>
    <div class="container-fluid mb-5">
        <!--  Navbar -->
        <nav class="navbar navbar-expand-sm navbar-dark" style="background-color:palevioletred">
            <a class="navbar-brand" href="/dashboard">Wedding Planner</a>
            <!-- Links -->
            <ul class="navbar-nav">
                <!-- Greeting -->
                <li class="nav-item">
                    <a class="nav-link">Hello @ViewBag.LogUser.FirstName</a>
                </li>
            </ul>
            <div class="ml-auto">
                <ul style="list-style-type: none;">
                    <li class="nav-item float-right">
                        <a class="nav-link text-white float-right" href="/logout">Log off</a>
                    </li>
                </ul>  
            </div> 
        </nav>
        <div class="container mx-auto mt-5">
            <div class="container row mt-5">
                <h2 class="mb-3 text-info">@ViewBag.Wed.Wedder1 and @ViewBag.Wed.Wedder2's Wedding</h2>
            </div>
            <hr>
            <div class="container p-3 col-6 mt-5 border rounded text-center">
                <div class="container my-3">
                    <u class="text-secondary">@ViewBag.Wed.Date.ToString("D")</u>
                </div>
                <h5><u class="text-primary">Guests</u></h5>
                <ul class="list-unstyled">
                    @{
                        @if (@ViewBag.Guests == null) {
                            <p>The wedding is currently having no guest.</p>
                        } else {
                            foreach (var guest in @ViewBag.Guests) {
                                <li class="text-secondary">@guest.User.FirstName @guest.User.LastName</li>
                            }
                        }
                    }
                </ul>
            </div>
        </div>
    </div>
</body>
</html>